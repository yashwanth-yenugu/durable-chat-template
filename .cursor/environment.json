{
	"install": "command -v bun >/dev/null || curl -fsSL https://bun.sh/install | bash -s bun-v1.2.15; export PATH=\"$HOME/.bun/bin:$PATH\" && bun install --frozen-lockfile",
	"terminals": [
		{
			"name": "wrangler-dev",
			"command": "export PATH=\"$HOME/.bun/bin:$PATH\" && bun run dev"
		}
	]
}
