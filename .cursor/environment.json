{
	"name": "roomgist",
	"install": "command -v bun >/dev/null || curl -fsSL https://bun.sh/install | bash -s bun-v1.3.14; export PATH=\"$HOME/.bun/bin:$PATH\" && bun install --frozen-lockfile",
	"ports": [
		{
			"name": "wrangler-dev",
			"port": 8787
		}
	],
	"terminals": [
		{
			"name": "wrangler-dev",
			"command": "export PATH=\"$HOME/.bun/bin:$PATH\" && bun run dev",
			"description": "Cloudflare Workers dev server (Wrangler) on http://localhost:8787"
		}
	]
}
