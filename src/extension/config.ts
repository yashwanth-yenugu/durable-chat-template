/** Cloudflare Worker host that serves the chat WebSocket backend. */
export const DEFAULT_CHAT_HOST =
	"durable-chat-template.templates.workers.dev";

/** Party name matching the Durable Object binding (lowercase). */
export const CHAT_PARTY = "chat";
